// import React from "react";

// import('./global.css');

// const App = React.lazy(() => import("./App"));

// const AppWrapper = (props) => (
//   <React.Suspense fallback="Loading App">
//       <App {...props}>{props.children}</App>
//   </React.Suspense>
// );

// console.log('app1: App', App);

// export default AppWrapper;

import('./bootstrap');

